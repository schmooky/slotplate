import { SoundKey } from '@lib/sounds/soundsKeys';

export const splashScreenSoundConfig = {
  ambientGainNode: [{ fileName: 'sfx_b_music_splash.mp3', soundName: SoundKey.MusicSplash, loop: true, volume: 1 }],
};
